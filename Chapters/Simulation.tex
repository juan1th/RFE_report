\section{COAXIAL RF LINE}
\subsection{Task description}
The objective of this task is to measure the standing wave ratio and the reflection coefficient of different loads connected to a coaxial transmission line using a signal analyser\cite{CXA_SA}, the loads to be used are: 

\begin{itemize}
	\setlength{\itemsep}{0pt}
	\setlength{\parskip}{0pt}
	\item Open
	\item Short
	\item 50$\Omega$
	\item 100$\Omega$
	\item 25$\Omega$
	\item Capacitive load
\end{itemize}

The transmission line is fed with a sine wave signal of +10dBm at 310 MHz from an RF signal generator\cite{RF_SG}.
\subsection{Schematic}
Figure~\ref{fig:5} shows the measurement configuration of the coaxial transmission line and a movable E-field probe.
\begin{figure}[ht!]
    \centering
    \includegraphics[page=1,width=0.8\linewidth]{src/diagrams/RF_1/Schematic_1.png}
    \caption{\centering Schematic of the measurement setup used, with RF coaxial line, E-field probe and the load.}
    \label{fig:5}
\end{figure}
\\
The schematic above shows the four main components used in this measurement setup. These are:
\begin{itemize}
	\setlength{\itemsep}{0pt}
	\setlength{\parskip}{0pt}
	\item The RF signal generator\cite{RF_SG}, which provides the excitation signal.
	\item The spectrum analyzer\cite{CXA_SA} with a movable E-field probe.
	\item The coaxial transmission line which is traced by the probe.
	\item The load at the end of the line; this can be replaced by any of the six terminations described earlier.
\end{itemize}

\newpage
\subsection{Formulas and calculations}
\subsubsection{\textbf{Calculation of the wave velocity:}}
The following formula \ref{eq:1_CO} is used to calculate the wave velocity $v$, obtained from \cite{lec-script}: \\
\begin{gather}
	v = f \cdot \lambda 							\label{eq:1_CO}
\end{gather}
$f$ (frequency) = 310MHz\\
$\lambda$ (wavelength) = $2 \cdot \Delta d $\\

The term $\Delta d$ is given by the difference between the points of two adjacent minima. \\For the different loads, this value (obtained from correlating the chart \ref{fig:chart1}, and the tables \ref{tab:power_plots_1} \& \ref{tab:power_plots_2}) is:
\begin{itemize}
	\setlength{\itemsep}{0pt}
	\setlength{\parskip}{0pt}
	\item Open: 48.3cm
	\item Short: 48.2cm
	\item Capacitive load: 48.5cm
\end{itemize} 

Averaging these values to minimize tiny individual measurement errors, obtained $\Delta d$ = 48.33cm.\\

Inferred wavelength: Because the standing-wave pattern repeats every half wavelength. \\This measured value for wavelength is obtained using the formula \ref{eq:4_CO} from: 
\begin{gather}
	\lambda = 2 \cdot \Delta d 						\label{eq:4_CO} \\
	\boxed{\lambda = 96.667cm}
\end{gather}

Hence,
Wave velocity (in m/s) is:
\begin{gather}
	v = 310 \times 10^6 \cdot 0.96451 				\nonumber \\
	\boxed{v = 2.9966 \times 10^8}							\label{eq:3_CO}
\end{gather}

\subsubsection{\textbf{Calculation of the voltage standing wave ratio and reflection coefficient magnitude:}}

Equation~\ref{eq:vswr_voltage} gives the definition of VSWR. Since the spectrum analyzer provides power values, VSWR can also be expressed in terms of the measured power ratio, as we know from\cite{lec-script}:
\begin{gather}
	\mathrm{VSWR} = \frac{V_{\max}}{V_{\min}} \label{eq:vswr_voltage} \\
	\mathrm{VSWR} = \sqrt{\frac{P_{\max}}{P_{\min}}} \nonumber
\end{gather}

Since $P_{\max}$ and $P_{\min}$ must be in linear units, the measured values in dBm are converted using:
\begin{gather}
	\frac{P_{\max}}{P_{\min}} = 10^{\frac{P_{\max,\mathrm{dBm}} - P_{\min,\mathrm{dBm}}}{10}} \nonumber
\end{gather}

Finally, VSWR for each termination is obtained by computing the square root of this term as:
\begin{gather}
	\boxed{\mathrm{VSWR} = 10^{\frac{P_{\max,\mathrm{dBm}} - P_{\min,\mathrm{dBm}}}{20}}} \label{eq:vswr_power}
\end{gather}

Using values from tables~\ref{tab:power_plots_1} and~\ref{tab:power_plots_2}, the calculation of VSWR (using the relation~\ref{eq:vswr_power}), and subsequently the magnitude of it's reflection coefficient ($|\mathrm{\underline{\rho_0}}|$) (using the relation below), is made possible (citing \cite{lec-script}):
\begin{gather}
	|\mathrm{\underline{\rho_0}}| = \frac{\mathrm{VSWR}-1}{\mathrm{VSWR}+1} \label{eq:rho_mag} \\
	\angle \rho = \text{from the position of the minima on the line} \label{eq:rho_phase}
\end{gather}

\subitem{\textbf{For Open load:}}
\begin{gather}
	\mathrm{VSWR} = 10^{\frac{-17.0 - (-73.0)}{20}}
	= 10^{\frac{56.0}{20}}
	= 10^{2.8}
	\approx 630.96 \label{eq:vswr_open}\\
	|\mathrm{\underline{\rho_0}}| = \frac{629.96}{631.96}
	\approx 0.997 \label{eq:mag_open}
\end{gather}

\subitem{\textbf{For a Short:}}
\begin{gather}
	\mathrm{VSWR} = 10^{\frac{-18.0 - (-64.0)}{20}}
	= 10^{\frac{46}{20}}
	= 10^{2.265}
	\approx 199.52 \label{eq:vswr_short} \\
	|\mathrm{\underline{\rho_0}}| = \frac{198.52}{200.52}
	\approx 0.99 \label{eq:mag_short}
\end{gather}

\subitem{\textbf{For 100$\Omega$ load:}}
\begin{gather}
	\mathrm{VSWR} = 10^{\frac{-20.8 - (-27.3)}{20}}
	= 10^{\frac{6.5}{20}}
	= 10^{0.325}
	\approx 2.11 \label{eq:vswr_100} \\
	|\mathrm{\underline{\rho_0}}| = \frac{1.11}{3.11}
	\approx 0.357 \label{eq:mag_100}
\end{gather}

\subitem{\textbf{For 25$\Omega$ load:}}
\begin{gather}
	\mathrm{VSWR} = 10^{\frac{-20.6 - (-28.7)}{20}}
	= 10^{\frac{8.1}{20}}
	= 10^{0.395}
	\approx 2.54 \label{eq:vswr_25} \\
	|\mathrm{\underline{\rho_0}}| = \frac{1.54}{3.54}
	\approx 0.435 \label{eq:mag_25}
\end{gather}

\subitem{\textbf{For 50$\Omega$ load:}}
\begin{gather}
	\mathrm{VSWR} = 10^{\frac{-22.9 - (-24.4)}{20}}
	= 10^{\frac{1.5}{20}}
	= 10^{0.075}
	\approx 1.19 \label{eq:vswr_50} \\
	|\mathrm{\underline{\rho_0}}| = \frac{0.19}{2.19}
	\approx 0.086	 \label{eq:mag_50}
\end{gather}

\subitem{\textbf{For Capacitive load:}}
\begin{gather}
	\mathrm{VSWR} = 10^{\frac{-18.3 - (-53.8)}{20}}
	= 10^{\frac{35.5}{20}}
	= 10^{1.775}
	\approx 59.57 \label{eq:vswr_cap} \\
	|\mathrm{\underline{\rho_0}}| = \frac{58.57}{60.57}
	\approx 0.967 \label{eq:mag_cap}
\end{gather}

\subsubsection{\textbf{Calculation of the reflection coefficient phase, and load impedances:}}
In order to obtain the angle of the reflection coefficient ($\delta_0$), one must use the following relation\cite{script} as laid out in the lab script: 
\begin{gather}
	\delta_n = (2n+1)\pi - 2\beta \mathrm{z_{min}} 		\label{eq:phase_refl} \\
	\mathrm{where, \quad}\beta = \frac{2\pi}{\lambda} 	\nonumber
\end{gather}

\newpage
Simplifying equation~\ref{eq:phase_refl}:
\begin{gather}
	\delta_0 = (2\cdot n+1)\pi - 2\beta \mathrm{z_{min}} 	\nonumber \\
	\boxed{\delta_0 = \pi - 4 \pi \cdot \frac{\mathrm{z_{min}}}{\lambda}} \quad
	\boxed{\delta_1 = 3\pi - 4 \pi \cdot \frac{\mathrm{z_{min}}}{\lambda}} \quad
	\boxed{\delta_2 = 5\pi - 4 \pi \cdot \frac{\mathrm{z_{min}}}{\lambda}} \label{eq:phase_refl_simplified}
\end{gather}

For the load impedances ($Z_L$), one can use the following formula\cite{script}:
\begin{gather}
	\mathrm{\underline{\rho_0}} = \frac{Z_L - Z_0}{Z_L + Z_0} 	\nonumber \\[4pt]
	\mathrm{\underline{\rho_0}} (Z_L + Z_0) = Z_L - Z_0 	\nonumber \\[4pt]
	Z_L = Z_0 \frac{1+\mathrm{\underline{\rho_0}}}{1-\mathrm{\underline{\rho_0}}} 	\label{eq:load_imp}\\
	\mathrm{where, \quad}\mathrm{\underline{\rho_0}} = |\mathrm{\underline{\rho_0}}|\cdot e^{j\delta_0} 	\nonumber
\end{gather}

% Using the tables~\ref{tab:power_plots_1} and~\ref{tab:power_plots_2}, the distance from the load to the position corresponding to the $0^{th}$ minima is obtained and used in the equation~\ref{eq:phase_refl_simplified} to obtain the angle of the reflection coefficient, and equation~\ref{eq:load_imp} is used for the load impedance calculation.

% The calculations were made with a Python script-\ref{lst:vswr-code} (placed in the appendix) this time, and the computed values are present in the table~\ref{tab:refl_phase_n_load}.

\subsection{Table with measurement results}
Tables~\ref{tab:power_plots_1} and~\ref{tab:power_plots_2} (placed in the appendix section of this document) present the measured power values at different positions along the coaxial RF line for different terminations.
\vspace{3mm}
\\The following table~\ref{tab:minimas} shows all the minima points on each of the 6 termination loads:
\input{src/assets/tabel/minimas}
%\input{src/assets/tabel/refl_phase_load}

These values are used to calculate the normalized phase of reflection coefficient (using formula~\ref{eq:phase_refl_simplified}).
\input{src/assets/tabel/refl_phase_values}

\newpage
Formula \ref{eq:load_imp} can be made use of to obtain the load impedance values. \\
The value of $Z_0$ is 50$\Omega$; the complex reflection coefficient must be computed to obtain the required load impedances. \\
Carrying out this computation using Python \ref{lst:load-code}, the values for complex reflection coefficient phase and load impedance for each of the 6 terminations can be represented as: \\
\input{src/assets/tabel/load_impedances}

The representation of these Load impedance values has been done on a smith chart in  figure \ref{fig:smith_chart1}.

\subsection{Measurement curve and diagrams} 
Figures~\ref{fig:screen1} shows the spectrum analyser output after making basic settings such as center, span, magnitude, sweep time, and residual bandwidth. The signal principal frequency is 310MHz and power is 10dBm.
\begin{figure}[ht!]
	\centering
	\includegraphics[page=4,width=0.7\linewidth]{src/diagrams/RF_1/Screen1_inv}
	\caption{\centering Spectrum analyser output on proper setting of required parameters.}
	\label{fig:screen1}
\end{figure}

\newpage
Illustrated in Figure~\ref{fig:chart1} is the measured location-dependent signal power values obtained along the coaxial transmission line with the RF Spectrum Analyser for termination loads: Open, Short and 100$\Omega$ loads. \\The measurements were taken over a range of 35.0cm to 167.0cm, measured from the termination load.
\begin{figure}[ht!]
    \centering
    \includegraphics[page=4,width=0.85\linewidth]{src/diagrams/RF_1/Chart_1}
    \caption{\centering Measured power values for open, short and 100$\Omega$ terminations.}
    \label{fig:chart1}
\end{figure}
\\Similar to \ref{fig:chart1}, illustrated in Figure~\ref{fig:chart2} is the measured location-dependent signal power values obtained along the coaxial transmission line with the RF Spectrum Analyser for termination loads: 25$\Omega$, 50$\Omega$ and purely capacitive loads.
\begin{figure}[ht!]
	\centering
	\includegraphics[page=4,width=0.85\linewidth]{src/diagrams/RF_1/Chart_2}
	\caption{\centering Measured power values for 25$\Omega$, 50$\Omega$ and capacitive load terminations.}
	\label{fig:chart2}
\end{figure}
\\A consolidated figure~\ref{fig:chart3} with all 6 measurements curves on the same chart is present in the appendix. 

\newpage
Illustrated in figure~\ref{fig:chart_voltages} is the calculated location-dependent voltage values along the coaxial transmission line.
\begin{figure}[ht!]
	\centering
	\includegraphics[page=4,width=0.85\linewidth]{src/diagrams/RF_1/Chart_voltages}
	\caption{\centering Calculated voltage over distance figures for all 6 load terminations.}
	\label{fig:chart_voltages}
\end{figure}

Using the values obtained, a Smith chart was created as can be seen in the below figure~\ref{fig:smith_chart1}. 
\begin{figure}[ht!]
	\centering
	\includegraphics[page=4,width=0.6\linewidth]{src/diagrams/RF_1/smith_chart}
	\caption{\centering The representation of reflection coefficients for all 6 loads on the Smith chart.}
	\label{fig:smith_chart1}
\end{figure}

\newpage
\subsection{Additional questions}
\textbf{Q. Is it possible to match all loads with an additional passive impedance matching
	network, consisting of transmission line and Capacitor?}\\
A. Analyzing this question from the perspective of individual loads and their ideal effects on a transmission line, stating once again that the line impedance is 50$\Omega$ and the objective is to match each load impedance to this value:
\begin{itemize}
	\item \textbf{50$\Omega$ load}: This load is already matched, nothing to do. \textbf{Possible}.
	\item \textbf{25$\Omega$ and 100$\Omega$ loads}: These are not matched, there are considerable reflections but it is rather simple to match them to the 50$\Omega$ load. \textbf{Possible}.
	\item \textbf{Open and short loads}: These loads represent extreme cases since both their reflection coefficient magnitudes are near 1, this cannot be matched only using a capacitor, one would require a real resistance.
	So, using only a transmission line and a capacitor, \textbf{it is not possible}.
	\item \textbf{Capacitive load}: A capacitive load falls at the outer circumference of the Smith chart. The capacitive part can be cancelled by an inductive reactance of the line, but the same phenomenon seen with open and short arises, it has no resistive part, and this cannot be compensated using only a simple passive network described here, hence, it is \textbf{not possible}.
\end{itemize} 

\textbf{Q. Calculate an impedance matching network (by using a transmission line + capacitor)
	for a load given by the supervisor.}\\
A. Matching the measured impedance $Z_L = 105.4 - 3j$ (which is $0.357\angle$-1.987) to $Z_0 = 50\Omega$ includes the following steps:

It shall be matched to a transmission line with characteristic impedance
\[
Z_0 = 50~\Omega
\]

\begin{enumerate}
	\item First step is to normalize the load impedance, one can refer to \cite{lec-script} equation:
	\[
	z_L = \frac{Z_L}{Z_0}
	= \frac{105.4-j3}{50}
	= 2.108-j0.06.
	\]
	
	\item Using the Smith chart, this point can now be used to draw the constant reflection magnitude circle,\\This circle intersects the 50$\Omega$ circle exactly at:
	\[
	z_\mathrm{m} =
	1+j0.77
	\]
	which corresponds to the impedance of:
	\[
	Z_\mathrm{m} =
	50+j38.55
	\]
	which can be represented as:
	\[
	0.357 \angle 68.8 ^\circ
	\]
	
	\newpage
	\item The length of the line is selected such that the real part of the impedance is compensated for:
	\[
	\Delta\angle = -1.987 - 68.8 = 289.2^\circ
	\]
	since from \cite{lec-script}:
	\[
	\Delta\angle = 2\beta l
	\]
	\[
	\beta = \frac{2\pi}{\lambda}
	\]
	
	One can simplify this to:
	\[
	l = \frac{\Delta\angle\cdot\lambda}{4\cdot\pi} = \frac{289.2\cdot\lambda}{720}
	\]
	\begin{gather}
		\boxed{l = 38.827cm}
	\end{gather}

	\item The capacitor compensates for the:
	\[
	Z_C=-38.55\Omega
	\]
	
	Using this value, the capacitance value of the required capacitor is:
	\[
	C = \frac{1}{2\pi f Z_C}
	\]
	\[
	\boxed{C = 13.3178pF} \label{capacitance-answer}
	\]


\end{enumerate}	

Thus, the characteristics of a matched load at 310 MHz using a 50$\Omega$ transmission line
is with a length of 38.82cm and a capacitor in series of approximately 13.31pF.

\begin{comment}
It shall be matched to a transmission line with characteristic impedance
\[
Z_0 = 50~\Omega
\]
at the frequency
\[
f = 310~\mathrm{MHz}.
\]

The wavelength is
\[
\lambda = 96.6667~\mathrm{cm} = 0.966667~\mathrm{m}.
\]

\begin{enumerate}
	\item Normalize the load impedance:
	\[
	z_L = \frac{Z_L}{Z_0}
	= \frac{105.4-j3}{50}
	= 2.108-j0.06.
	\]
	
	\item A 50$\Omega$ transmission line transforms the normalized load
	impedance according to \cite{script}, page 15.:
	\[
	z_\mathrm{in} =
	\frac{z_L+j\tan(\beta l)}
	{1+jz_L\tan(\beta l)}
	\]
	where
	\[
	\beta=\frac{2\pi}{\lambda}=6.5
	\]
	
	\item The line length is selected such that the real part of the
	normalized input admittance is equal to one:
	\[
	y_\mathrm{in}=\frac{1}{z_\mathrm{in}}=1-j0.7643
	\]
	
	This condition is obtained for:
	\[
	l=0.3432\cdot\lambda
	\]
	
	Using the given wavelength:
	\[
	l=0.3432\cdot 0.966667
	=0.3317~\mathrm{m}
	\]
	
	Therefore, the required transmission-line length is:
	\[
	\boxed{l\approx33.17~\mathrm{cm}}
	\]
	
	\item At this position, we calculate:
	\[
	Y_\mathrm{in}
	=\frac{1-j0.7643}{50}
	=0.02-j0.01529
	\]
	
	The capacitor must now provide the opposite susceptance:
	\[
	B_C=+0.01529
	\]
	
	Since
	\[
	B_C=2\pi fC,
	\]
	the required capacitance is:
	\[
	C=\frac{0.01529}{2\pi\cdot310\cdot10^6}
	\]
	
	Thus:
	\[
	\boxed{C\approx7.85~\mathrm{pF}}
	\]
	
	\item Finally the total input admittance after adding the capacitor comes out to be:
	\[
	Y_\mathrm{total}
	=(0.02-j0.01529)+j0.01529
	=0.02
	\]
	
	Therefore:
	\[
	Z_\mathrm{total}=\frac{1}{Y_\mathrm{total}}
	=\frac{1}{0.02}
	=50~\Omega
	\]
\end{enumerate}


Thus, the characteristics of a matched load at 310 MHz using a 50$\Omega$ transmission line
is with a length of 33.17 cm and a shunt capacitor of approximately 7.85 pF.
\end{comment}

\FloatBarrier
\subsection{Discussion} 
\begin{itemize}
  \item \textbf{Wave velocity and wavelength}:\\
  The calculation of wave velocity was done after determining the wavelength. \\Both of these figures seem plausible and valid for the measurements made.
  
  \item \textbf{VSWR \& Reflection coefficient magnitude}:\\
  The calculated VSWR and reflection coefficient magnitude values can be analysed as:
	  \begin{itemize}
	 	\item \textbf{For open and short loads}: Extremely high VSWR, as expected, yielding a near full reflection in either cases.
		\item \textbf{For 50$\Omega$ impedance}: Matched impedance resulting in ideal VSWR, ideally should be 1.0 but stands at 1.19, likely due to parasitic effects of the setup. Very small reflection coefficient magnitude.
		\item \textbf{For 25$\Omega$ impedance}: Small VSWR but significant enough to cause reflections. Ideally should be 2 but reaches 2.54. Reflection coefficient roughly in the same neighbourhood but notable shift from 0.333 ideal scenario.
		\item \textbf{For 100$\Omega$ impedance}: Same expectations as 25$\Omega$, but delivers a much better result. VSWR reaches 2.11, close enough to expectations. Reflection coefficient of 0.357, significantly better than 25$\Omega$ result.	
		\item \textbf{For a purely capacitive load}: Expectations are to see a high VSWR, but lower than open or short loads, still yielding a near 100\% reflection, the achieved results seem fair enough.
	  \end{itemize}

  \item \textbf{Reflection coefficient phase and Load impedances}:\\
  The calculated reflection coefficient phases and the resulting load impedances can be analyzed as follows:
	\begin{itemize}
		\item \textbf{For open and short loads}: The obtained distances for the voltage minima were relatively consistent, but the final calculated load impedances deviated noticeably from the ideal. Theoretically, an open or short should yield an infinite or zero impedance with purely reactive edge-cases, but this is not seen perfectly.
		
		\item \textbf{For 50$\Omega$ impedance}: This is the only load where the phase calculations cannot be heavily relied upon. Because the load is nearly matched, the standing wave minima are extremely shallow and difficult to accurately locate. Consequently, the phase calculations yielded varying results. The final plotted point on the Smith chart is only close to the centre due to the definitively small reflection coefficient magnitude, rather than an accurate phase measurement.
		
		\item \textbf{For 25$\Omega$ impedance}: Ideally, this load should plot on the horizontal line (just like $100\Omega$ and $50\Omega$) on the real axis inside the Smith chart ($25 + j0\,\Omega$). However, any slight error in locating the exact minimum position physically translates to an noticeable phase shift in the calculations. This introduces a parasitic reactive component, pushing the plotted point off the real axis.
		
		\item \textbf{For 100$\Omega$ impedance}: This carries the same expectations as the 25$\Omega$ load but performed noticeably better. It yielded a primarily resistive value near 100$\Omega$ with only a minor resistive and reactive offset, placing it fairly accurately in the expected right-hand portion of the real axis.
		
		\item \textbf{For a purely capacitive load}: Ideally, this load should yield a purely negative reactance and sit firmly in the lower half of the Smith chart. However, our calculated impedance deviated significantly from this fundamental expectation. (explanation later)
	\end{itemize}
	
	\textbf{Verdict}: To try and explain the offsets observed for the extreme cases (open and short), one could attribute this to the length of the load adapters, as well as measurement setup factors like cable attenuation, and parasitic inductance at the terminations. \\Even tiny errors in the reflection coefficient, like from (ideal case of) 1 to (measured instance of) 0.997 for an open circuit, causes a huge shift in the location of the same in the Smith chart because the reflection coefficient changes from 0 + j349.96 -> 3.75 + j349.92.
	
	However, the most notable anomaly is the capacitive load being plotted in the incorrect quadrant of the Smith chart. Despite rechecking all calculation and reviewing the measurements multiple times, and also consulting external references, the exact cause of this unexpected behaviour could not be definitively identified.
\end{itemize}

